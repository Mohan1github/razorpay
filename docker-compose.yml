version: '3.9'

services:
  app:
    build: .
    container_name: razorrisk-ai
    ports:
      - '5000:5000'
    environment:
      FLASK_ENV: production
      HOST: 0.0.0.0
      PORT: 5000
      DATABASE_PATH: /data/razor_risk.db
    volumes:
      - ./data:/data
    restart: unless-stopped
